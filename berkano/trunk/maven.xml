<?xml version="1.0" encoding="UTF-8" ?>
<project default="multiproject" xmlns:maven="jelly:maven" xmlns:artifact="artifact">

<!--
    <goal name="build-all">
        <maven:reactor basedir="${basedir}"
                       includes="*/project.xml"
		       excludes="user-xstream/project.xml"
                       goals="clean,jar:install"
                       banner="Building"
                       ignoreFailures="false"/>
    </goal>

    <goal name="deploy-all">
        <maven:reactor basedir="${basedir}"
                       includes="*/project.xml"
                       excludes="user-xstream/project.xml"
                       goals="clean,jar:deploy"
                       banner="Building"
                       ignoreFailures="false"/>
    </goal>

    <goal name="snapshot-all">
        <maven:reactor basedir="${basedir}"
                       includes="*/project.xml"
                       goals="clean,jar:deploy-snapshot"
                       banner="Building"
                       ignoreFailures="false"/>
    </goal>
-->

    <goal name="xdoclet-tld">
        <taskdef name="xdoclet" classname="org.xdoclet.ant.XDocletTask" classpathref="maven.dependency.classpath"/>
        <xdoclet>
            <fileset dir="${pom.build.sourceDirectory}">
                <include name="**/*.java"/>
            </fileset>
            <component classname="org.generama.JellyTemplateEngine"/>
            <component classname="org.xdoclet.plugin.web.TaglibPlugin"
                       destdir="${basedir}/target/classes/META-INF"
		       jspversion="2.0"
                       includelisteners="false"/>
        </xdoclet>
    </goal>

<!--
    <preGoal name="java:compile">
        <attainGoal name="xdoclet"/>
    </preGoal>
-->

<!--
    <goal name="deploy" prereqs="jar:jar">
        <echo message="Deploying ............................. ${maven.build.dir}/${maven.final.name}.jar"/>
        <artifact:deploy artifact="${maven.build.dir}/${maven.final.name}.jar" type="jar" project="${pom}"/>
    </goal>
-->
</project>
