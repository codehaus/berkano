<?xml version="1.0" encoding="ISO-8859-1"?>

<project>
    <extend>../project.xml</extend>
    <id>berkano-tags-kiss</id>
    <currentVersion>${berkanoVersion}</currentVersion>
    <name>Simple tags tag library</name>
    <description>The Simple tags tag library is a collection of very small tags, with a very etroit focus blabla.</description>
    <shortDescription>Simple tags tag library</shortDescription>

    <dependencies>
        <!-- tests only -->
        <dependency>
            <groupId>berkano-tags</groupId>
            <artifactId>berkano-tags-test</artifactId>
            <version>${berkanoVersion}</version>
        </dependency>
    </dependencies>
</project>
